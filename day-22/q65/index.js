// Function accepting two number and returns a number
function getRemainder(dividend, divisor) {
    // Performs division and returns their remainder
    return dividend % divisor;
}
console.log(getRemainder(12, 7)); // 5
