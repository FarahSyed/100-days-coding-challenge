// A function that takes a string and returns a number
function stringLength(str) {
    // Returns the length of the string
    return str.length;
}
console.log(stringLength("The sun will rise, and we will try again")); // 40
