// A function that takes a string
function changeCases(str) {
    var upperCase = str.toUpperCase(); // Converts to uppercase
    var lowerCase = str.toLowerCase(); // Converts to lowercase
    // Logs both cases
    console.log("Uppercase:", upperCase);
    console.log("Lowercase:", lowerCase);
}
changeCases("Sunrise");
