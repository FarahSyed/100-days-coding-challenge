// Function accepts two numbers and returns a number
function addTwoNumbers(num1, num2) {
    // Adds two numbers
    var sum = num1 + num2;
    return sum;
}
// Logs the value of the function invoked with 2 parameters having type number
console.log(addTwoNumbers(2, 2));
