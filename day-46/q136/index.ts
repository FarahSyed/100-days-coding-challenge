// An array of fruits
const fruits: string[] = ["Apple", "Mango", "Banana", "Watermelon"];

// For loop that loops through the fruits array
for (let index = 0; index < fruits.length; index++) {
    const element = fruits[index];
    console.log("Element at index:", index, "is", element);   // Keep track of elements and their indices
}