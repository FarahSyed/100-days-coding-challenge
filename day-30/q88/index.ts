// A function that takes a string and returns a number
function stringToNumber(str: string): number {
    // Converts the provided string to number
    return Number(str);
}

console.log(stringToNumber("123"));