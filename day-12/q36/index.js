// Function that accepts a size and the text message
(function make_shirt(size, text) {
    // Print the size and the message
    console.log("The shirt size is ".concat(size, " and its text is \"").concat(text, "\"."));
})("Large", "Peace on Earth!"); // Imediately invoked function
