// A function that takes a string and returns a string
function extractSubstrings(str) {
    // Returns string after specifying its start and end points
    return str.substring(0, 10);
}
console.log(extractSubstrings("This is TS - TypeScript"));
