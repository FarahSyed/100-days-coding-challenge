var names = ['Ali', 'Charlotte', 'Alina', 'Sam']; // Storing friends names in an array
// Printing personalised messages for each name using for loop
for (var i = 0; i < names.length; i++) {
    var element = names[i];
    console.log("Hi ".concat(element, ", How are you doing?"));
}
;
