let names: string[] = ['Ali', 'Charlotte', 'Alina', 'Sam'];   // Storing friends names in an array

// Printing personalised messages for each name using for loop
for (let i = 0; i < names.length; i++) {
    const element = names[i];
    console.log(`Hi ${element}, How are you doing?`);
};