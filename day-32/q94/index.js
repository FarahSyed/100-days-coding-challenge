// An Array of strings storing different fruits
var fruits = ["Mango", "Cherry", "Banana"];
// Getting length of each word from an array of words by mapping through it
var wordLength = fruits.map(function (word) { return word.length; });
console.log(wordLength);
